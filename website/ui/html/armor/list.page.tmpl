{{define "armors/list"}}

{{template "header" .}}

    <!-- <div class="album py-4 bg-secondary"> -->
        <!-- <div class="container">
            <div class="row">
                {{range .Armors}}
                <div class="col-md-3 d-flex justify-content-start">

                    <div class="card mb-2 shadow-sm bg-dark">
                        <div class="card-body">
                            <img class="card-text mx-auto d-block" src={{.ImageUrl}} alt="Hylian-hood" width="80" height="80">
                            <a href="/armor/view/{{.ID.Hex}}" class="btn btn-muted text-light p-1" role="link">{{.Name}}</button>
                        </div>
                    </div>
                </div>
                {{end}}
            </div>
        </div> -->
    <div class="album py-4 bg-secondary">
        <div class="d-flex flex-row justify-content-start align-content-between flex-wrap" style="padding-left: 80px;">
             
            {{range .Armors}}
            <div class="card mb-3 bg-dark"  style="width: 8.5rem;height: 12rem;">
                <div class="card-body">
                    <img class="card-img mx-auto d-block" src={{.ImageUrl}} alt="Hylian-hood" width="80" height="80">
                    <div class="card-title text-light">{{.Name}}</div>
                    <a href="/armor/view/{{.ID.Hex}}" class="btn btn-primary text-light p-1">view</a>
                </div>
            </div>
            {{end}}
        </div>

    </div>

{{template "footer" .}}

{{end}}